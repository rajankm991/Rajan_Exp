// ============================================================================
// PASSWORD CONFIG — shared by transactions.html and cashflow.html
// ============================================================================
// This is the ONE place to change the password for those two protected pages.
// Both pages read PASSWORD from this file, so editing it here updates the
// password everywhere — no other file needs to change.
//
// HOW TO CHANGE THE PASSWORD:
// Just edit the value below, save this file, and push it to your repo.
//
// Note: this is a plain-text password in a public GitHub file, checked only
// in the browser (GitHub Pages has no server to check it against). It stops
// casual visitors from seeing the page, but anyone who opens this file
// directly (e.g. via the repo's file list) can read the password, and
// data.json is still a public file at its own URL either way.
// ============================================================================

const PASSWORD = 'Rajan@991';
